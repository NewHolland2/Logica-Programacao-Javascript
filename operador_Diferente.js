console.log(5 != 5);     // false (os valores são iguais)
console.log(5 != '5');   // false (os valores são iguais, mesmo com tipos diferentes)
console.log(5 != 10);    // true (os valores são diferentes)
console.log(true != 1);  // false (os valores são iguais)

const x = 10;
console.log(x != 10);    // false (os valores são iguais)
console.log(x != '10');  // false (os valores são iguais, mesmo com tipos diferentes)
